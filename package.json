{
  "name": "@litecanvas/plugin-asset-loader",
  "version": "0.6.3",
  "description": "Plugin to help load external assets in litecanvas games",
  "author": "Luiz Bills <luizbills@pm.me>",
  "license": "MIT",
  "homepage": "https://github.com/litecanvas/plugin-asset-loader#readme",
  "bugs": {
    "url": "https://github.com/litecanvas/plugin-asset-loader/issues"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/litecanvas/plugin-asset-loader.git#main"
  },
  "main": "src/index.js",
  "keywords": [
    "litecanvas",
    "joystick",
    "touch",
    "gamedev"
  ],
  "scripts": {
    "dev": "esbuild src/index.js --bundle --watch --outfile=dist/dist.js --servedir=.",
    "build": "esbuild src/index.js --bundle --minify --outfile=dist/dist.js",
    "prepare": "npm run build"
  },
  "devDependencies": {
    "esbuild": "^0.20.1"
  },
  "peerDependencies": {
    "litecanvas": "*"
  },
  "files": [
    "dist",
    "src"
  ]
}
